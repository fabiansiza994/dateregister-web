// Central API base URL for all services
// Change this in one place to repoint environments (dev/staging/prod)
export const API_BASE = 'http://localhost:8081';

// Optional: If any service needs a different host/port later, add overrides here, e.g.:
// export const SALES_API_BASE = API_BASE;
// export const AUTH_API_BASE = API_BASE;
// For now all use API_BASE.
