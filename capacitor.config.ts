import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fmsp.dataregisterapp',
  appName: 'dataregisterapp',
  webDir: 'dist/dataregister-web/browser',
};

export default config;
